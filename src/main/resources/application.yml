spring:
  r2dbc:
    url: r2dbc:postgres://localhost:5432/postgres?schema=anime
    username: postgres
    password: admin
logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG

server:
  error:
    include-stacktrace: on_param


